name: Action workflow for my Hello World Dockerfile,it is simples
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build the Docker image
      - run: docker build . --file ../action-a/Dockerfile --tag my-image-name:$(date +%s)